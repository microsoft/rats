class ProviderNamespaces:
    SERVICES = "services"
    GROUPS = "groups"
    CONTAINERS = "containers"
    CONFIGS = "configs"
    FALLBACK_SERVICES = "fallback-services"
    FALLBACK_GROUPS = "fallback-groups"
    FALLBACK_CONFIGS = "fallback-configs"
